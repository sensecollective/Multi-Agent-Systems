<h1>Multi-Agent Information Retrieval for Touristic Recommendations</h1>

For running the programe followe the next steps:
1. run command "sudo python configure.py localhost"
2. run command "sudo python runspade.py"
3. run denisa_main.py
You will obtain 2 files:
a) URLs.txt file where there are written URLs found by the FinderAgent
b) analyzerResults.csv where you can find the list of touristic activities/places suitable for the user

Also, the programe have multiple outputs in console, including the list of the activities that the ProblemSolverAgent gives to the user.